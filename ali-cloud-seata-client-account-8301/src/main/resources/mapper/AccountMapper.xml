<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ali.cloud.seata.mapper.AccountMapper">
    <resultMap id="BaseResultMap" type="com.ali.cloud.seata.entity.AccountDTO">
        <id column="id" property="id" />
        <result column="user_id" property="userId"/>
        <result column="money" property="money" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,money
    </sql>
    <update id="updateAccountMemony">
        update account_tbl set money = money - ${money}  where user_id = ${userId}
    </update>

    <select id="selectAccount" resultType="com.ali.cloud.seata.entity.AccountDTO">
        select id,user_id,money from account_tbl where user_id = ${userId}
    </select>
</mapper>